from setuptools import setup, find_packages

setup(
    name="kyklos-sdk",
    version="0.1.0",
    description="Python SDK for writing Kyklos pipeline steps",
    packages=find_packages(),
    python_requires=">=3.10",
)
